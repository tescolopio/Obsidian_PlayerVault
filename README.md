# Obsidian_PlayerVault
a plugin that duplicates the vault locally, uses regex to strip out any text wrapped in specific tags (e.g., %%SECRET%% or > [!gm-only]), removes orphan links, and compiles the remaining notes into a static HTML wiki.
